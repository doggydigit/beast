#include "base.cc"

void init(Application* app)
{
	
	// Ici on peutr par exemple créer une ruche et l'ajouter au terrain
	//Hive* h(new Hive(...);
	//wxGetApp().getMap()->addHive(h);
	// Ajouter des abeilles
	//h->addBee(new ScoutingBee(...);
	//h->addBee(new WorkingBee(...);
	// etc.
	// A adapter bien sur a votre propre code
}
